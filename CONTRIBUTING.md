# Contributing

Read `AGENTS.md` before changing Forge. Keep reusable UI in `commonMain`, preserve caller-owned
state and copy, use tokens for design values, and provide tests plus showcase coverage for public
components.

Run the focused module checks while developing and all Android, iOS, ABI, skill, and publication
smoke checks before preparing a release.
